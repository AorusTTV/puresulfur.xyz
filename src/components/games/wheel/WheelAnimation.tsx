
import React from 'react';

interface WheelAnimationProps {
  isSpinning: boolean;
}

export const WheelAnimation: React.FC<WheelAnimationProps> = ({ isSpinning }) => {
  // No animations for now - clean slate
  return null;
};
